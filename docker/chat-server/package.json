{
  "name": "tsn-chat-server",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Single-page chat front-end for the ts-networks authoring agent. Holds one Claude Agent SDK session in-process inside the container; browser talks to it over SSE + POST.",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "smoke": "node smoke-test.mjs",
    "test:reducers": "node reducer-test.mjs",
    "test": "node smoke-test.mjs && node reducer-test.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.177"
  }
}
