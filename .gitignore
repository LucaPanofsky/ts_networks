.clj-kondo/
.lsp/
node_modules/
dist/
src/data-network/parser.js
src/data-network/parser.terms.js
report/
*.db
dev
# Claude Code local state (ignore all of .claude EXCEPT committed, shared skills)
.claude/*
!.claude/skills/
# Playwright MCP scratch (screenshots, snapshots, console logs — regenerable)
.playwright-mcp/
# Agent scratch workspace (PDFs/txt the agent materializes — not source)
WORKSPACE/
# Maintenance-analysis: the tool (repo_workspace/analysis/*.ts) and reports under
# outputs/ are tracked — reports are committed on demand. Only jest's coverage dump
# is generated noise.
coverage/

# Agent host-side work dir (bind-mounted to /workspace)
tsn-work/

# Local deployment secrets (holds ANTHROPIC_API_KEY) — never commit; .env.example is tracked
.env
docker/.env

# Vision white paper — work in progress, commit deliberately when ready
design/proof-of-reasoning.html

# Meta-language abstraction exploration — local scratch, not for the repo
gavalang_exploration/
