# TS NETWORK UI — STALE, not part of the alpha

> **Status:** STALE — abandoned static assets (`index.html`, fonts, CSS) for the
> browser editor in `src/editor/` + `src/ui-server/`.

**Not maintained, not part of the alpha surface, and not to be revived.** The supported
way to work with `.tsn` programs is the CLI scripts (`npx tsx scripts/*.ts`) and the MCP
server (`npm run mcp`) — see the repo [README](../README.md) **Getting Started**.
